#* * * * * echo "Hello world ENV=$ENV" >> /root/logscron.log 2>&1
*/5 * * * * /root/namecheap-update-dns.sh >> /root/namecheap-update-dns.log
# Don't remove the empty line at the end of this file. It is required to run the cron job

